// Create a copy of this file named `config.ts` and replace the configuration values below with your own email addresses and filters

export const config = {
    "SendToEmails": ["my-email@email.com"],
    "SendFromAddress": "my-email@email.com",
    "FilterKeywords": ["delta", "skyteam", "jetblue"]
}